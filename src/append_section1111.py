"""收束版对照表落到两个地方（**表只住一处**）：

① 探索文档 §11.11 = 权威表（带版本、失败原因、复算命令、边界）
② `docs/结论.md` 的对照台一节 = 只放一行指针 + 三条要点，不复制数字（一数一处）
"""
from pathlib import Path

DOC = Path(r"C:\dev\finlab-ml\docs\探索-判断器的效率（GBDT vs Jev vs LLM）.md")
CONC = Path(r"C:\dev\finlab-ml\docs\结论.md")

t = DOC.read_text(encoding="utf-8")
assert "### 11.11" not in t

TABLE = """

### 11.11 收束：一张表（**全部标清题面版本与失败原因**，2026-09-25 11:2x）

| 判断器 | 输入 | 准确率 | 覆盖率 | 空答的**原因**（逐类数清） | 不确定带 [0.2,0.8] | 单条延迟 |
|---|---|---|---|---|---|---|
| **GBDT**（LightGBM 多分类） | 68 个财务列 + 行业 | **65.8% ± 3.1**（多数类基线 53.3% · macro-F1 0.357） | 100% | —— | **14.17%** | **2 ms** |
| `glm-5.3` | 公告全文 · **题面已修** | 97.94%（95/97） | 97/120 | **配额 429 × 22** · 撞顶 1 | 0.00% | 5.0 s |
| `glm-5.3` | 公告全文 · 旧题面 | 98.17%（107/109） | 109/120 | **撞顶 × 7** · 网络失败 × 4 | 0.00% | 7.6 s |
| `kimi-k3` | 公告全文 · **题面已修** | **98.33%（118/120）** | **120/120** | 无 | 0.83% | 5.5 s |
| `kimi-k3` | 公告全文 · 旧题面 | 95.83%（115/120） | 120/120 | 无 | 4.17% | 3.0 s |
| （参照）`jev-1.13` | 公告全文 | **未测** | 0 | **上游 503 `Endpoint is unavailable`**（通宵 59 发探测无一通） | —— | —— |

**三条从这张表里读出来的、我敢写进结论的**：

1. **判断器的价值不是"更准"，是"能读语义"**：GBDT 只看得到 65.8%（它手里没有"原因"这条信息，比瞎猜高 12.5 点）；
   判断器读到 98% 量级 —— 而这份差距**全部来自"公告正文里那句原因"**，不是模型容量。
2. **失败形态三家各不相同，而"覆盖率"这个数必须连着原因报**：glm 会**想不完**（撞 token）、被**额度**掐（429）、**网络**断；
   kimi 一次没空过；GBDT 从不失败但会**自信地猜错**。⇒ **"覆盖率 90.8% vs 100%"这种比较，不把原因摆出来就是误导。**
3. **校准只有一家能摊开**：GBDT **14.17%**、kimi **0.83%**、glm **0.00%**（自家 LLM 基线 0.0%）。
   ⇒ 想做「低置信转人工」，**唯一站得住的那条路是 GBDT 的不确定带**（见 §11.8 的路由设计）。

**复算**：`python src/card_prefix_fix_120.py`（前后四轴卡）· `src/split_blank_causes.py`（空答按原因拆）·
`src/compare_judges.py`（两家并排）· `src/diag_gbdt_as_judge.py`（GBDT 那一格）。
⚠️ **额度状态**：这一格今天用掉 Go 套餐 **705 次调用 / 85.1 万 prompt / 28.5 万 completion tokens（429 共 142 次）**
⇒ **今天 glm/kimi 再跑会被 429 挡**；Jev 走按量付费那条，不受此影响，但它自己的上游还在 503。
"""

DOC.write_text(t.rstrip() + TABLE, encoding="utf-8")
print("§11.11 已落 · 文档", len(DOC.read_text(encoding="utf-8")), "字符")

c = CONC.read_text(encoding="utf-8")
OLD = "→ [`docs/探索-判断器的效率（GBDT vs Jev vs LLM）.md`](探索-判断器的效率（GBDT vs Jev vs LLM）.md) · §8 场景清单 · §10 金标第一跑"
NEW = ("**已跑完第一个真第三方判断器**（`glm-5.3` / `kimi-k3`，各 120 道；判错的 2 条有一部分是**我题面的洞**，已修并重跑）——\n"
       "**收束表见 §11.11**（含 GBDT 那一格、题面版本、以及每一格空答的**原因**：撞顶 / 配额 429 / 网络）。⚠️ 表只住那一处，这里不复制数字。\n"
       "→ [`docs/探索-判断器的效率（GBDT vs Jev vs LLM）.md`](探索-判断器的效率（GBDT vs Jev vs LLM）.md) · §8 场景清单 · §10 金标第一跑 · "
       "§11 首次真跑 · §11.7 两家并排 · **§11.11 收束表**")
assert OLD in c
CONC.write_text(c.replace(OLD, NEW, 1), encoding="utf-8")
print("结论.md 指针已加 · 含 '§11.11 收束表'：", "§11.11 收束表" in CONC.read_text(encoding="utf-8"))
