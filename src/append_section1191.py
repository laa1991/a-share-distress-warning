"""把「修题面前后」的实测结果补进 §11.9，并对 §11.3 / §11.7 / §11.8 的"同一条缝"结论做更正。"""
from pathlib import Path

P = Path(r"C:\dev\finlab-ml\docs\探索-判断器的效率（GBDT vs Jev vs LLM）.md")
t = P.read_text(encoding="utf-8")
assert "### 11.9.1" not in t

BLOCK = """

#### 11.9.1 修完题面之后：**那条"缝"基本消失了**（同一批 41 条 · 前后对照）

| 模型 | 改题面前（旧题面） | 改题面后 |
|---|---|---|
| `glm-5.3` | 答出 **34/41**（7 空）· 对 33 · **错 1** | 答出 **40/41**（1 空）· 对 39 · **错 1** |
| `kimi-k3` | 答出 41/41 · 对 36 · **错 5** | 答出 41/41 · 对 39 · **错 2** |

**那 5 条"两家都错"的，改题面之后 10/10 全对**：

| 案子 | gold | glm 前→后 | kimi 前→后 |
|---|---|---|---|
| `002528` | B | A → **B** | A → **B** |
| `300125` | A | 空 → **A** | E → **A** |
| `300225` | B | 空 → **B** | A → **B** |
| `600745` | B | 空 → **B** | A → **B** |
| `603557` | B | 空 → **B** | A → **B** |

**残余只剩 2 条，而且两家残余完全相同**：
- `603007`（gold=A）：glm 空 / kimi 答 B（conf 0.85）；
- `002717`（gold=B）：两家都答 A（conf 0.97 / 0.95）—— ⚠️ **这一条正是读者在 v5 报告里点名"两种读法给出不同字母"的那条**（位置规则⇒B，本次叠加触发⇒A）⇒ **它撞在真值本身的歧义上**，不是模型错。
判据：`src/compare_prefix_fix.py`（只比同一批 41 条，不跨批）。

⚠️ **因此要更正三处此前的结论**：

1. **§11.3 / §11.7 写的"两个模型都错在同一对邻居（内控意见 vs 财报意见）"** —— 在修好题面之后**不成立了**：
   那 5 条共有错里，**4 条是照我题面的洞答的**（B 定义漏"财报非标意见"）、1 条是照我写错的规则答的（"取较早出现的"）。
   ⇒ **"同一条缝"至少有三种来源：题面定义漏项 / 规则有歧义 / 任务真难** —— 我此前只想到第三种，并把它写进了文档。
2. **§11.8 写的"三个独立方法同时指到同一条缝"** —— 只能保留**GBDT 那一半**：它**没读题面**，
   它的 36/41 落在 A↔B 是"手里没有原因这条信息"的结果；**两个 LLM 那半被撤回**（它们读了我的错题面）。
3. **本节的旧读数（glm 98.2% / kimi 95.8%）是在"题面有洞"的条件下取得的** ⇒ 它们**不是这两家在这道题上的真实水平**；
   已用修好的题面重跑全 120 道（`--out-tag prefixfix120`），最终数字以那一版为准。

⇒ 一句话：**这一格真正量到的东西，不是"模型有多准"，而是"我的题面值多少分"** —— 修半句定义，共有的错从 5 条变 0 条。
"""

P.write_text(t.rstrip() + BLOCK, encoding="utf-8")
print("追加 §11.9.1 ✓ · 文档", len(P.read_text(encoding="utf-8")), "字符")
