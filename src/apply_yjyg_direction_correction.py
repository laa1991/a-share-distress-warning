"""把「修订方向」这条读数的**更正**铺进所有引用了它的地方（一次做完，写完逐处回读）。

为什么：金标 30 条（我读的）抓出分类器的一个系统性缺陷 —— 找不到「修正后」分界时两段退化成同一段，
于是 v1 == v0 ⇒ 必然读成「不变」。原发布值 48.4% 覆盖 / 291:168:148 / 翻面 20.1% 里，**148 条「不变」有 113 条是伪影**。

修好后：覆盖 **36.1%（453）** · 坏→更坏 **288** · 好→更好 **165** · 不变 **0（本方法判不了）** · 翻面 **120（26.5%）**；
金标（n=30，我逐条读）与规则一致 **88.9%（16/18）**，剩余 2 条错在**邻近科目被当净利**（营收/净资产）。

用法: python apply_yjyg_direction_correction.py        # 打印每处命中数，不静默跳过
"""
from __future__ import annotations

from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
DESK = Path.home() / "Desktop" / "求职岗位"

OLD_TABLE = """| ① 数值对比（只在含「归属…净利润」的行里取**区间**，前段 vs 后段） | 覆盖率 **48.4%**（607/1,255）：**坏→更坏 291 · 好→更好 168 · 不变 148**；**盈亏翻面 122 条（20.1%）** |"""
NEW_TABLE = """| ① 数值对比（只在含「归属…净利润」的行里取**区间**，前段 vs 后段） | 覆盖率 **36.1%**（453/1,255）：**坏→更坏 288 · 好→更好 165 · 不变 0**；**盈亏翻面 120 条（26.5%）** |"""

OLD_NUM = """**两个数字值得记住**：
- **下修 : 上修 ≈ 291 : 168 ≈ 1.7 : 1** —— 修订**倾向于报坏消息**，这与"预告这格几乎等于答案"叠加后，
  意味着**修订事件本身就是一条负向信号**，滚动链应当给它更高的优先级；
- **约 20% 的修订是盈亏翻面**（122 条）—— 这是"名单从预测变已知"里**信息量最大的那一类**。"""
NEW_NUM = """**两个数字值得记住**：
- **下修 : 上修 ≈ 288 : 165 ≈ 1.75 : 1** —— 修订**倾向于报坏消息**，这与"预告这格几乎等于答案"叠加后，
  意味着**修订事件本身就是一条负向信号**，滚动链应当给它更高的优先级；
- **约 26.5% 的修订是盈亏翻面**（120 条）—— 这是"名单从预测变已知"里**信息量最大的那一类**。"""

OLD_LESSON = """⇒ v2 改成**只在含「归属…净利润」的行里取区间**，覆盖率从 75.5% 降到 48.4% —— **降下来的是可信度换来的**；
并用勾选框做第二个独立读数交叉验证（98.7% 一致）。**宁可覆盖率低，也不报一个抽错的数。**"""
NEW_LESSON = """⇒ v2 改成**只在含「归属…净利润」的行里取区间**，覆盖率从 75.5% 降到 36.1% —— **降下来的是可信度换来的**；
并用勾选框做第二个独立读数交叉验证（98.4% 一致，仅在两读法都出值的子集上）。

### 7.1.1 ⚠️ 更正（2026-09-25 凌晨，金标抓出）

**怎么发现的**：给判断器建考卷时做了 30 条**人工裁定**（我逐条读摘录，`src/fill_rev_gold.py`），
结果与分类器标签只有 **56.7%** 一致，且 13 条不一致里 **12 条是分类器说「不变」而公告明显改了**。

**根因（一条代码缺陷，不是什么洞察）**：分类器按「修正后的预计业绩」这一行切前/后两段；
**切不到那一行时，两段退化成同一段** ⇒ 抽出的前后区间**逐字相同** ⇒ 必然判「不变」。
于是「不变」这个桶里装的多数是**抽取失败**。

**改动（`src/classify_yjyg_rev_dir.py`）**：① 分界找不到 ⇒ **判不了**（不再退化成同一段）；
② 前后区间**文本相同** ⇒ 判不了；③ 中位数**数值相同** ⇒ 判不了（一份修正公告里数字一字不差几乎总是同句读了两遍）；
④ 退一步搜索时**排除净资产/所有者权益/营业收入/每股**这类邻近科目。

**三个口径的账（同一条命令、同一批正文）**：

| 口径 | 覆盖率 | 坏→更坏 | 好→更好 | 不变 | 翻面 | 金标一致率（n=30） |
|---|---|---|---|---|---|---|
| 原发布值 | 48.4%（607） | 291 | 168 | **148** | 20.1% | **56.7%**（判不了也计入分母） |
| 修 ①（分界缺失⇒判不了） | 38.9% | 288 | 165 | 35 | 24.6% | 76.2% |
| **修 ①–④（现值）** | **36.1%（453）** | **288** | **165** | **0** | **26.5%** | **88.9%**（16/18） |

**结论怎么变**：
- **比例稳**：下修:上修 1.7:1 → **1.75:1** ⇒ 「修订倾向于报坏消息」这条**站得住**；
- **「不变」这一类本方法判不了**（现在的 0 不是"没有不变"，是**判不出**）—— 要判它得读原因说明或勾选框；
- **剩下的错是"邻近科目当净利"**（金标 18 条里 2 条：营收、净资产被当成净利润）⇒ 真实一致率 **≤88.9%**，
  且错误方向**系统性偏"改好"**（把营收/净资产的增长读成净利增长）；
- ⇒ **被丢弃的那 62% 里，「判不了」不是随机分布**（金标样本里落在「不变」伪影上的 10 条**全部**是"改坏"）
  ⇒ **真实的下修:上修 应当比 1.75:1 更偏下修**。要知道偏多少，得换读法（更好的抽取，或一个判断器）。"""

# ——— 落点：每处 (文件, 旧串, 新串)；跑完逐处打印命中数，不许静默跳过 ———
TARGETS: list[tuple[Path, str, str]] = [
    (ROOT / "docs" / "设计-业绩预告滚动链.md", OLD_TABLE, NEW_TABLE),
    (ROOT / "docs" / "设计-业绩预告滚动链.md", OLD_NUM, NEW_NUM),
    (ROOT / "docs" / "设计-业绩预告滚动链.md", OLD_LESSON, NEW_LESSON),
    (ROOT / "docs" / "结论.md",
     """- 读 1,255 份正文判方向（数值对比 × 勾选框两个独立读数，**一致率 98.7%**）：
  **下修 291 : 上修 168 ≈ 1.7 : 1**，**盈亏翻面 122 条（20.1%）** ⇒ **修订事件本身是一条负向信号**。""",
     """- 读 1,255 份正文判方向（数值对比 × 勾选框两个独立读数，**一致率 98.4%**）：
  **下修 288 : 上修 165 ≈ 1.75 : 1**，**盈亏翻面 120 条（26.5%）** ⇒ **修订事件本身是一条负向信号**。
  ⚠️ **2026-09-25 更正**：原发布值（291:168:148 / 覆盖 48.4%）里**「不变」148 条有 113 条是抽取伪影**（切不到「修正后」那一段时前后退化成同一段）；
  修后覆盖 **36.1%**、**「不变」这一类本方法判不了**；用 30 条人工金标复核，与规则一致 **88.9%**（剩余错在"邻近科目被当净利"，方向偏"改好"）。"""),
    (ROOT / "docs" / "结论.md",
     """方向读数覆盖率 **48.4%**（只在含「归属…净利润」的行里取区间 —— 覆盖低是为了抽得准）；""",
     """方向读数覆盖率 **36.1%**（只在含「归属…净利润」的行里取区间 —— 覆盖低是为了抽得准）；
且**「不变」这一类判不了**（方法只能给"改坏／改好／判不了"三态），人工金标一致率 **88.9%**（n=18 可判）；"""),
    (ROOT / "docs" / "探索-判断器的效率（GBDT vs Jev vs LLM）.md",
     """标签本身来自规则抽取（覆盖率 48.4%、与勾选框一致 98.7%），""",
     """标签本身来自规则抽取（更正后覆盖率 **36.1%**、人工金标一致率 **88.9%**）；"""),
    (ROOT / "docs" / "探索-判断器的效率（GBDT vs Jev vs LLM）.md",
     """滚动链方向覆盖率 48.4%。""",
     """滚动链方向覆盖率 **36.1%**（更正后；「不变」那一类本方法判不了）。"""),
    (ROOT / "docs" / "探索-判断器的效率（GBDT vs Jev vs LLM）.md",
     """我的正则：48.4% 覆盖、与勾选框一致 **98.7%**""",
     """我的正则：**36.1%** 覆盖、人工金标一致 **88.9%**"""),
    (ROOT / "docs" / "探索-判断器的效率（GBDT vs Jev vs LLM）.md",
     """规则标签（1,255 份；勾选框交叉一致 **98.7%**） | 规则：覆盖 **48.4%**""",
     """规则标签（1,255 份；人工金标一致 **88.9%**） | 规则：覆盖 **36.1%**"""),
    (DESK / "LightGBM落地（财务风险预警）-新增候选素材-2026-09-24-金融线.md",
     """**下修:上修 1.7:1**、盈亏翻面 20.1%）⇒""",
     """**下修:上修 1.75:1**、盈亏翻面 26.5%；人工金标复核一致率 88.9%）⇒"""),
]


def main() -> int:
    bad = 0
    for path, old, new in TARGETS:
        if not path.exists():
            print(f"❌ 缺文件：{path}")
            bad += 1
            continue
        t = path.read_text(encoding="utf-8")
        n = t.count(old)
        if n != 1:
            print(f"❌ {path.name}：命中 {n} 次（期望 1）—— 未改动")
            bad += 1
            continue
        path.write_text(t.replace(old, new, 1), encoding="utf-8")
        print(f"✅ {path.name}：1 处已改")
    print(f"\n{'全部落地' if bad == 0 else f'{bad} 处未落地，逐条看上面'}（{len(TARGETS) - bad}/{len(TARGETS)}）")
    return 0 if bad == 0 else 1


if __name__ == "__main__":
    raise SystemExit(main())
