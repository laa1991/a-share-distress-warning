"""追加 §11.8：GBDT 在同一批 120 道上跑一遍（"GBM vs 判断器"那一格）。"""
from pathlib import Path

P = Path(r"C:\dev\finlab-ml\docs\探索-判断器的效率（GBDT vs Jev vs LLM）.md")
t = P.read_text(encoding="utf-8")
assert "### 11.8" not in t

SECTION = """

### 11.8 把 GBDT 放到同一批 120 道上（"那 GBM 呢"那一格）

**同一批题、同一份裁决真值**，但**给 GBDT 的只有它能拿到的东西**：那只股票在该公告日之前**已披露**的
**68 个财务列 + 行业**（PIT 纪律：只取 `实际披露日 ≤ 案件日期` 里最近一期）——**没有公告正文**。
判据 `src/diag_gbdt_as_judge.py`（5 折 × 3 种子，`data/judge_arena/gbdt_as_judge.json`）。

| 判断器 | 手里有什么 | 一致率 / 准确率 | 覆盖率 | **不确定带 [0.2,0.8]** | 延迟（单条） |
|---|---|---|---|---|---|
| **GBDT**（LightGBM 多分类） | **68 个财务列 + 行业** | **65.8% ± 3.1%**（种子 0.617/0.667/0.692）· macro-F1 **0.357** | 100% | **14.17%** | **2 ms** |
| `glm-5.3` | 公告全文 | **98.2%**（答出的 107/109） | 90.8% | 0.00% | 7,554 ms |
| `kimi-k3` | 公告全文 | **95.8%**（115/120） | 100% | 4.17% | 2,986 ms |
| （参照）多数类基线「全猜 B」 | —— | 53.3% | —— | —— | —— |

**三件事，每件都有判据：**

1. **GBDT 比"全猜 B"高 12.5 个点，但离判断器差 30 个点** ⇒ 不是它弱，是**它手里没有"原因"这条信息**——
   这正是这条线的老结论（**触发规则不在报表里**）在分类任务上的又一次现身。⇒ **这一格不能读成"谁更强"，
   只能读成"两种输入各能支撑到哪"**。
2. **它把 A/B 互相搞混**：41 个错里 **36 个落在 A↔B**（A→B 18 · B→A 18），而**两个判断器和我的关键词规则也错在同一对邻居**上
   ⇒ **三个独立方法同时指到同一条缝** ⇒ "内控意见 vs 财报意见"是**任务级难点**，这条现在有三份证据。
3. **唯一一个"知道自己不确定"的家伙**：它的不确定带 **14.17%**，而两个判断器是 **0.00% / 4.17%**。
   ⇒ 直接长出一个设计：**拿 GBDT 的不确定带做路由** —— 只把"它自己不确定"的那 ~14% 送去给判断器逐条读，
   其余 86% 直接落类。**成本降一个量级，而送出去的那批恰好是最需要语义的地方**（也在那条 A/B 缝上）。
   （这条是**设计，不是已验读数**：路由之后端到端值多少分没量过。）

⚠️ **边界**：① n=120、四类、E 类只有 2 条 ⇒ **macro-F1 0.357 就已经说明少数类不可用**，这一格只读"量级与形状"；
② 3 个种子的极差 0.075（0.617–0.692）⇒ **单种子读数不可信**，报的是分布；
③ 与判断器**不同输入**（数字 vs 正文）⇒ 表里的"一致率"与"准确率"是同一把尺（都对着 v6-120 裁决真值），
但**不等于同一个任务**：GBDT 是在"没有原因信息"的条件下尽力，判断器是在**读了原因**之后作答。
"""

P.write_text(t.rstrip() + SECTION, encoding="utf-8")
print("追加 §11.8 ✓ · 文档", len(P.read_text(encoding="utf-8")), "字符")
