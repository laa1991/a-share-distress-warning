"""§11.10：修好题面之后的全 120 道读数（两家 × 前/后），以及一个**还没分开的两种解释**。"""
from pathlib import Path

P = Path(r"C:\dev\finlab-ml\docs\探索-判断器的效率（GBDT vs Jev vs LLM）.md")
t = P.read_text(encoding="utf-8")
assert "### 11.10" not in t

BLOCK = """

### 11.10 修好题面之后的全 120 道（两家 × 前/后）—— **一家涨、一家掉，而掉的原因还没分开**

| 模型 | 题面 | 覆盖率 | 一致率（答出的） | 一致率（空算错） | 延迟中位 | 置信度中位 | 不确定带 |
|---|---|---|---|---|---|---|---|
| `kimi-k3` | 旧（有洞） | 120/120 | 95.83%（115/120） | 95.83% | 2,986 ms | 0.980 | 4.17% |
| `kimi-k3` | **新（修好）** | 120/120 | **98.33%（118/120）** | **98.33%** | 5,454 ms | 0.980 | 0.83% |
| `glm-5.3` | 旧（有洞） | 109/120（90.8%） | 98.17%（107/109） | 89.17% | 7,554 ms | 0.980 | 0.00% |
| `glm-5.3` | **新（修好）** | **97/120（80.8%）** | 97.94%（95/97） | **79.17%** | 5,047 ms | 0.980 | 0.00% |

- **kimi 的读数变干净了**：错 5 → **2**，一致率 **95.83% → 98.33%**（+2.5 点）⇒ 题面修好确实帮到它。
  残余 2 条是：`603007`（gold=A 答 B）与 `002717`（gold=B 答 A，**这条是真值本身有歧义的那条**）。
- **glm 反而掉**：覆盖率 90.8% → **80.8%**（空 11 → **25** 条）。⚠️ **两个解释我分不开**：
  ① **题面变长**（多了一整条定义 + 规则），它要读的东西更多 ⇒ 更容易撞满 6000 token；
  ② **时段/上游波动** —— 同一端点今天一直在 503，且**同一题同预算在 temperature 0 下会一次答出、一次答不出**（`600745` 在探针里答出、在 120 道里空）。
  判据：**已在跑一次同题面的重复**（`--out-tag prefixfix120r2`）。若重复也落在 80% 上下 ⇒ ① 或"稳定地差"；
  若回到 90% 上下 ⇒ **波动就是主因，这个 10 点不能算在题面头上**。
- 这是这一节第二次撞到同一件事：**「同一份题面、同一个模型、两次跑出来不一样」** —— 所以凡是拿这类读数做前后对照，
  **必须先量一次同条件的重复**（我这次是先写了对照才发现缺这一格，只能补跑）。

⚠️ **本节所有旧读数（§11.1 / §11.7 / §11.8 里那张四轴卡）都是"旧题面"条件下的**；
引用它们时必须带这半句：**旧题面的 B 定义漏了"财报非标意见"，规则与真值口径不一致**。
"""

P.write_text(t.rstrip() + BLOCK, encoding="utf-8")
print("追加 §11.10 ✓ · 文档", len(P.read_text(encoding="utf-8")), "字符")
